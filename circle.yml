machine:
  timezone:
    America/Chicago

dependencies:
  post:
    - 'bundle exec appraisal install'

database:
  override:
    - 'bundle exec appraisel rake --rakefile test/dummy/Rakefile db:setup'

test:
  override:
    - 'bundle exec appraisal rake'
